<?xml version="1.0" encoding="UTF-8"?>
<feed xmlns="http://www.w3.org/2005/Atom" xml:base="{{ site.href | escape }}/">
  <title>{{ site.title | escape }}</title>
  <id>{{ site.href | escape }}{{  page.href | escape }}</id>
  <link href="{{ site.href | escape }}/" />
  <link rel="self" href="{{ site.href | escape }}{{ page.href | urlencode | escape }}/" />
  {% if page.posts | length > 0 %}
  <updated>{{ page.posts[0].timestamp.isoformat() }}</updated>
  {% endif %}
  <author>
    <name>Json Lee</name>
  </author>

  {% for post in page.posts %}
  <entry>
      <title>{{ post.title | escape }}</title>
      {% if post.subtitle is not none %}<subtitle>{{ post.subtitle | escape }}</subtitle>{% endif %}
      <link rel="alternate" href="{{ site.href | escape }}{{ post.href | escape }}" />
      <id>{{ site.href | escape }}{{ post.href | escape }}</id>
      <published>{{ post.timestamp.isoformat() | escape }}</published>
      <updated>{{ post.updated.isoformat() | escape }}</updated>
      <author>
        <name>Json Lee</name>
      </author>
      <summary type="xhtml"><div xmlns="http://www.w3.org/1999/xhtml">
        {{ post.summary() | safe }}
      </div></summary>
  </entry>
  {% endfor %}

</feed>
